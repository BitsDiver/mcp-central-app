# ──────────────────────────────────────────────────────────────
# mcp-central-app — Frontend release
#
# Triggers when a version tag (v*) is pushed.
# Builds the Vite app and publishes the `dist/` folder as a
# tarball attached to a rolling GitHub release (tag: frontend-latest).
# The backend CI pulls this artifact to embed the UI in its Docker image.
# ──────────────────────────────────────────────────────────────

name: Release frontend

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write   # required to create / update GitHub Releases

jobs:
  build-and-release:
    name: Build & publish
    runs-on: ubuntu-latest

    steps:
      # ── Checkout ────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4

      # ── Node + pnpm ─────────────────────────────────────────
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      # ── Install & build ─────────────────────────────────────
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      # ── Package dist/ ────────────────────────────────────────
      - name: Create dist archive
        run: tar -czf dist.tar.gz -C dist .

      # ── Publish (rolling "frontend-latest" release) ──────────
      - name: Update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: frontend-latest
          name: "Frontend — latest build (${{ github.ref_name }})"
          prerelease: true
          body: |
            Built from tag **${{ github.ref_name }}** (commit `${{ github.sha }}`).
          files: dist.tar.gz
